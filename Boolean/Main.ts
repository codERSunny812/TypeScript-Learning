let pending : boolean;

pending=true;

pending = false;


// we can manipulate the boolean value using the booleean opreator

let value : boolean = true;

let changedValue : boolean = !value;


let hasError : boolean = true;
let completed : boolean = false;

let result = hasError && completed

console.log(result)

let result2 = hasError || completed

console.log(result2)



